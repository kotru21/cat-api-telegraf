# App
PORT=5200
WEBSITE_URL=http://localhost
NODE_ENV=development
# SESSION_SECRET is required (no default value)
# Use a strong random string, minimum 10 characters
SESSION_SECRET=generate-your-own-secret-key-here

# Bot
BOT_ENABLED=false
# BOT_TOKEN=your-telegram-bot-token

# Web server
WEB_ENABLED=true

# External APIs
CATAPI_KEY=your-cat-api-key

# Redis (optional - in-memory fallback when disabled)
# Set REDIS_ENABLED=true to use Redis for sessions and rate limiting
REDIS_ENABLED=false
# REDIS_URL=redis://localhost:6379
# REDIS_ALLOW_SELF_SIGNED=false

# WebSocket configuration (optional)
# WS_MAX_CONNECTIONS_PER_IP=5
# WS_MESSAGE_RATE_LIMIT=10

# Database (Prisma)
# Runtime and CLI must point to the same DB file.
# Recommended: keep the DB inside the prisma folder
DATABASE_URL="file:./prisma/main.db"
# When using the sqlite file locally (development/test), run:
#   npm run prisma:generate:dev
#   npm run prisma:migrate:dev:sqlite